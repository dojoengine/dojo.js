{
    "name": "dojo.js",
    "type": "module",
    "packageManager": "bun@1.2.19",
    "workspaces": {
        "packages": [
            "packages/*",
            "examples/*"
        ],
        "catalog": {
            "starknet": "^8.1.2",
            "@starknet-io/get-starknet-core": "4.0.7",
            "@starknet-react/core": "^5.0.1",
            "@starknet-react/chains": "^5.0.1",
            "@starknet-io/types-js": "^0.8",
            "@types/react": "^18.3.1",
            "@types/react-dom": "^18.3.1",
            "react": "^18.3.1",
            "react-dom": "^18.3.1"
        }
    },
    "scripts": {
        "build:deps": "turbo build:deps",
        "build:wasm": "turbo build:wasm",
        "build:packages": "turbo build --filter=\"@dojoengine/*\"",
        "build:examples": "turbo build --filter=\"example-*\"",
        "build": "turbo build",
        "clean": "bash ./scripts/clean.sh",
        "biome:format": "bunx @biomejs/biome format packages examples .json --write",
        "biome:format:check": "bunx @biomejs/biome format packages .json examples",
        "biome:lint": "bunx @biomejs/biome lint packages examples --write",
        "biome:lint:check": "bunx @biomejs/biome lint packages examples",
        "biome:check": "bunx @biomejs/biome check packages examples --write",
        "format": "turbo format",
        "format:check": "turbo format:check",
        "lint": "turbo lint",
        "lint:check": "turbo lint:check",
        "changeset": "changeset",
        "version": "changeset version",
        "prepare:publish": "bun scripts/prepare-publish.ts",
        "restore:packages": "bun scripts/restore-packages.ts",
        "release": "bun run build:deps && bun run prepare:publish && changeset publish && bun run restore:packages --cleanup",
        "release:dry-run": "bun run build:deps && bun run prepare:publish && changeset publish --dry-run && bun run restore:packages --cleanup",
        "dev": "turbo watch dev --concurrency 20",
        "dev:deps": "turbo watch build:deps --concurrency 20",
        "docs": "turbo run docs --ui stream && typedoc --entryPointStrategy merge 'docs-json/*.json'",
        "typedoc": "typedoc --options typedoc.json && typedoc",
        "test": "turbo test",
        "test:watch": "turbo watch test"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.1.3",
        "@commitlint/cli": "^18.6.1",
        "@commitlint/config-conventional": "^18.6.3",
        "@typhonjs-typedoc/typedoc-theme-dmt": "^0.2.3",
        "husky": "^9.1.7",
        "lerna": "^8.2.3",
        "tsup": "^8.5.0",
        "turbo": "^2.5.5",
        "typedoc": "^0.26.11",
        "typedoc-plugin-coverage": "^3.4.1",
        "typedoc-plugin-missing-exports": "^3.1.0"
    },
    "dependencies": {
        "@changesets/cli": "^2.29.5",
        "@dojoengine/recs": "2.0.13"
    },
    "commitlint": {
        "extends": [
            "@commitlint/config-conventional"
        ]
    }
}
